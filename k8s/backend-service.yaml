apiVersion: v1
kind: Service
metadata:
  name: mern-auth
  namespace: assessment
spec:
  type: NodePort
  selector:
    app: mern-auth
  ports:
    - port: 80
      targetPort: 9191
      nodePort: 30080
